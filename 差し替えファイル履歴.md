# 差し替えファイル履歴（サーバーへ個別アップロード用）

ZIPまるごとではなく、下記ファイルだけを **同じ階層のまま** サーバー公開フォルダ
（`public_html/`）へ上書きアップロードすれば反映されます。

新しい変更は上に追記していきます。

---

## 2026-07-14（その30）

### 3方針を正式文面に差し替え＋上部ナビに「ポリシー」ドロップダウン追加＋ヘッダーを共有JS化（銀行口座開設向け）

> **サーバへアップするのは下記の4ファイルだけ**です（同じ階層のまま上書き）。
> `js/main.js` ＋ `security.html` ＋ `sitepolicy.html` ＋ `privacy.html`

- **`security.html`・`sitepolicy.html`・`privacy.html`** → 正式文面に全面差し替え（制定日：2026年7月7日）。
  - 情報セキュリティ基本方針（前文＋7項目）／サイトポリシー（前文＋8項目・利用目的は箇条書き）／
    プライバシーポリシー（前文＋7項目・取得利用は箇条書き、窓口 info@revenge.co.jp）。
- **`js/main.js`** → ヘッダーを共有化（「ヘッダーの正（最新版）」を main.js が持つ方式）。
  - 全ページ読み込み時に、ヘッダーの中身を最新版へ差し替え。**上部ナビに「ポリシー」プルダウン**
    （情報セキュリティ基本方針／サイトポリシー／プライバシーポリシー）を追加。会社情報プルダウンと
    同じ挙動（PC開閉・モバイルはアコーディオンで排他開閉）。
  - **今後ヘッダーを変えるときは、この `js/main.js` 1ファイルだけ差し替えればよい**（全HTMLを
    1枚ずつ直す必要なし）。各HTMLに焼き込まれたヘッダーは JS 無効時・クローラ向けのフォールバック
    として残しています（見た目は同じ）。
  - `js/main.js` を上げれば、他ページを差し替えなくても**全ページのナビに「ポリシー」が反映**されます。
- **`?v=`（キャッシュ更新用の番号）を廃止**。以前は CSS/JS を変えるたび全HTMLの `?v=` が変わり
  「全ファイル差し替え」になっていました。今後は**アセット（CSS/JS）を変えても HTML は一切変わりません**。
  キャッシュ更新はサーバの更新日時（ETag / Last-Modified）判定に任せます。
  - ※この `?v=` 撤去のため、リポジトリ上は今回一度だけ全HTMLに差分が出ますが、**サーバへ上げるのは
    上記4ファイルのみ**で大丈夫です（既存ページの古い `?v=` は無害。footer の各ポリシーリンクからも到達可能）。
- **デジタルソリューション（IT）事業は今回は出しません**（3事業のまま）。事業ページ追加・RAG埋め込みは
  口座開設完了後に対応予定。※その27（IT事業追加）・その28（カード4列）は今回のライブには含めない構成に戻しています。

---

## 2026-07-10（その28）

### 事業カードを横一列（4列）に
- **`css/style.css`** → PC（1000px以上）の事業カードを3列→**4列**に変更し、4事業を横一列で表示。タブレット2列・スマホ1列は従来どおり。
- **全HTML** → `?v=` ハッシュ更新のみ（内容不変）。同じ階層へ上書き。

---

## 2026-07-10（その27）

### 事業「デジタルソリューション事業」を追加
- **`index.html`・`services.html`** → 事業カードに4枚目「デジタルソリューション事業」を追加。
  事業一覧の見出し文も「3事業→4事業」に更新。
- **`service/service-04.html`（新規）** → `public_html/service/` へ。詳細ページ：
  - 提供メニュー：①ホームページ制作（オリジナル）②埋め込み型RAG（パッケージ）
    ③イベント運営 事前決済システム（パッケージ）④新規システム開発
  - **料金（税込・保守は月額）**：
    HP 初期5〜30万円／月額保守1〜10万円、RAG 初期5〜20万円／月額保守2〜10万円、
    イベント事前決済 初期2万円／月額保守5,000円、新規開発 お見積り（要相談）
  - **ご相談の流れ（無料）**：お問い合わせ（無料・ヒアリングシート）→無料でプロトタイプ作成
    →本契約の判断→満足いただけるまで修正
  - **「作って終わりにしません」**：公開後も保守・運用・集客改善まで継続支援
- **`contact.html`・`contact.php`** → お問い合わせ種別に「デジタルソリューションについて」を追加
  （詳細の相談ボタンから自動選択 `?type=digital`）。
- **`sitemap.xml`・`llms.txt`** → デジタルソリューション事業を反映。
- 既存3事業の詳細ページ・他ページは不変（`?v=` も不変）。

---

## 2026-07-10（その26）

### ニュースをWordPressに一本化（静的ニュースを撤去）
ニュースの実体を WordPress（`config.js` の `MISIMA_WP_BASE="/wp"`）に集約。
- **`index.html`・`news.html`** → `public_html/` へ上書き
  - トップ／一覧の `.p-top-news__list`（WPが差し替える枠）は維持。中身を静的サンプルから
    **「ただいまニュースを準備中です。」のフォールバック**に変更（WP未到達／JS無効時のみ表示。
    WPに投稿があれば `loadWpNews` がWP投稿で自動差し替え、リンクもWP記事URLに）。
- **`news/news-20260707.html`（静的サンプル詳細）を削除** → サーバーの
  **`public_html/news/news-20260707.html` も削除してください**（重要）。
- **`sitemap.xml`** → 静的ニュース詳細URLを除外（自動）。
- **`robots.txt`** → WP側サイトマップ参照 `Sitemap: https://revenge.co.jp/wp/wp-sitemap.xml` を追記。
- 他ページ（会社情報・事業・ポリシー等）は**完全に不変**（`?v=` も変化なし）。

> ニュース記事は今後 WordPress 管理画面から追加すると、トップ／一覧に自動反映されます。

---

## 2026-07-10（その25）

### コード整理（可読性・保守性／コメント追加）※ブラウザ表示は一切変更なし
表示・文章・挙動は変えず、不要コードと古いコメントを整理し、初心者向けの説明コメント（処理内容・主要変数の定義場所と役割）を追加。
- **`js/main.js`** → `public_html/js/main.js`
  - 未使用の死にコードを削除：旧トップイントロ `.p-intro` ブロック（約146行）、旧MVの `.p-top-mv__roman` タイピング、未使用変数（`heroScroll`/`stackEl`/`rainbowOnce`）。※対応する要素は全ページに存在せず、実行されていなかったもの。
  - 生きているロジックは**一切変更せず**、正確なコメント／セクション見出し／変数の役割説明を追加。
- **`css/style.css`** → `public_html/css/style.css`
  - 使われていない `.p-top-mv*`・`.p-intro*` のルールと、それだけが使う `@keyframes`（`caret`/`scrollLine`/`roulettePop`/`lineRainbow`/`intro*`/`arrowBounce`）を削除。※対応要素0件のため描画に影響なし。
- **全HTML** → `?v=` ハッシュ更新のみ（本文・見た目は不変）。同じ階層へ上書き。
- 検証：全14ページの `<body>` は `?v=` を除き変更前と完全一致／主要ページの前後スクショが pixel 一致／ヒーロー・ナビ・カウントアップ・フォームの動作を確認。

---

## 2026-07-10（その24）

### 5行を「1行ずつ自動で流れる」に変更＋流れ切るまでスクロール遷移をロック
- **`js/main.js`** → `public_html/js/main.js`
  - ⑤5行を**1行ずつ時間差で自動表示**（約0.7秒間隔のカスケード）に変更（スクロール量比例は撤去）。
  - **自動で流れている間はスクロールしても遷移しない**（全5行が出そろうまでロック）。流れ切ったら余分な入力は破棄し、改めてスクロールで⑥キャッチへ。
  - ③複合→④矢印の0.5秒自動、SCROLL↓の必要時表示などは維持。
- **全HTML** → `?v=` ハッシュ更新。同じ階層へ上書き。（CSS変更なし）

---

## 2026-07-10（その23）

### SEO・AIO対策（※ブラウザ表示は一切変更なし＝`<head>`内の不可視メタと補助ファイルのみ）
全14ページの `<body>`（デザイン・文章・挙動）は変更前とバイト単位で完全一致を確認済み。CSS/JSも未変更のため `?v=` ハッシュも不変。
- **全HTML** → 同じ階層へ上書き。追加したのは `<head>` 内の不可視要素のみ：
  - `canonical`（正規URL）、`robots`（`max-image-preview:large, max-snippet:-1` ＝リッチ表示/AI要約向け）
  - OGP（`og:*`）・Twitterカード（SNS/リンク共有時のみ表示、ブラウザ本体の表示は不変）
  - 構造化データ JSON-LD：`Organization`（社名・所在地・代表者・連絡先）、トップに `WebSite`、下層に `BreadcrumbList`、ニュース詳細に `NewsArticle`
- **`robots.txt`**（新規）→ `public_html/robots.txt`：全許可＋主要AIクローラ（GPTBot / ClaudeBot / PerplexityBot / Google-Extended 等）を明示許可（AIO）＋ sitemap 参照。
- **`sitemap.xml`**（新規）→ `public_html/sitemap.xml`：全ページの一覧（lastmod / priority）。
- **`llms.txt`**（新規）→ `public_html/llms.txt`：LLM向けのサイト要約（AIO標準フォーマット）。

> ※ Search Console へ `sitemap.xml` を登録すると反映が早まります（任意）。

---

## 2026-07-10（その22）

### 5行の1行あたりのスクロール量をさらに短く
- **`js/main.js`** → `public_html/js/main.js`
  - 1行進めるのに必要なスクロール量を **約350px → 約220px** に短縮。
  - 「量に比例（大きくスクロール＝複数行）」「5行後は一時停止せずキャッチへ」の挙動は維持。
- **全HTML** → `?v=` ハッシュ更新。同じ階層へ上書き。（CSS変更なし）

---

## 2026-07-10（その21）

### 5行の1行あたりのスクロール量を短く
- **`js/main.js`** → `public_html/js/main.js`
  - 1行進めるのに必要なスクロール量を **約600px → 約350px** に短縮（少ないスクロールで1行進む）。
  - 「量に比例（大きく／激しくスクロールすれば複数行まとめて）」「5行後は一時停止せずキャッチへ」の挙動は維持。
- **全HTML** → `?v=` ハッシュ更新。同じ階層へ上書き。（CSS変更なし）

---

## 2026-07-10（その20）

### 矢印を「複合の完了後」に出す＋5行をスクロール量に比例して表示
- **`js/main.js`** → `public_html/js/main.js`
  - **④矢印**：これまで③複合とほぼ同時に出ていた（複合スタックの出現アニメ約1.55秒の最中に矢印が重なっていた）。
    → **複合が出そろってから約0.5秒後に矢印を自動表示**するよう修正（この待機中はスクロール↓も非表示）。
  - **⑤5行**：これまで「1スワイプ＝1行」に固定されていた（行ごとのロックのため）。
    → ロックを外し、**累積スクロール量に比例**して表示。少しスクロール＝1行、大きく・激しくスクロールすれば**一度に2〜数行**まとめて表示。1行あたりの必要量を増量（約600px）。
    → **5行が揃っても一時停止せず**、そのままスクロールを続ければキャッチ（THE NEXT STAGE）へ流れる。
- **全HTML** → `?v=` ハッシュ更新。同じ階層へ上書き。
- ※今回 `css/style.css` の変更はありません。

---

## 2026-07-10（その19）

### ヒーローを「スクロール操作式」に戻す（全自動再生を撤回）＋要望反映
全自動再生（その16〜18）をやめ、自分のペースでスクロールして進む方式に戻しました。
そのうえで以下を反映：
- **`js/main.js`** → `public_html/js/main.js`
  - 段階ごとの進み方：
    - ①失敗（ロード時に自動表示）
    - 失敗 →②再挑戦：スクロール（普通・1スクロール＝1ステップ）
    - ②再挑戦 →③複合：スクロール（普通）
    - ③複合 →④矢印：**自動**（複合が出た**0.5秒後に矢印が自動表示**）
    - ④矢印 →⑤5行：**多めのスクロールで1行ずつ表示**（時間差の自動表示ではない。各行に多め）
    - 5行目 →⑥キャッチ：スクロール（普通・自動遷移なし）
  - **SCROLL↓は「操作待ちの時だけ」表示**（遷移中・複合→矢印の自動待ち中・本編解放後は非表示）。
  - 上スクロールで前段へ戻れる（キャッチ到達前）。キャッチ後はスクロールで本編へ。
- **`css/style.css`** → `public_html/css/style.css`
  - SCROLL↓の表示条件を `.p-hero.is-scrollhint`（操作待ち）に変更。
  - **ヘッダーのロゴ＋「Re:venge」を少し左へ**（`.l-header__logo` を左に微移動）。
- **全HTML** → `?v=` ハッシュ更新。同じ階層へ上書き。

---

## 2026-07-10（その18）

### スクロールで全スキップされる問題を修正（5行より前は厳禁）
- **`js/main.js`** → `public_html/js/main.js`
  - これまで自動再生中どこでスクロールしてもキャッチまで一気にスキップしていた。
  - 変更：**5行より前（失敗→再挑戦→複合→矢印）はスクロール厳禁**（無視＝自動再生に任せる）。
    **5行表示中のみ**スクロールでTHE NEXT STAGEへスキップ可。キャッチ後はスクロールで本編へ。
- **全HTML** → `?v=` ハッシュ更新。同じ階層へ上書き。

---

## 2026-07-10（その17）

### 自動再生のテンポを速く（各段階の間・5行の間隔を0.5秒に）
- **`js/main.js`** → `public_html/js/main.js`
  - 各段階の間（AUTO_READ）を0.5秒、5行の表示間隔を1秒→**0.5秒**に短縮。全自動での完走が約14秒に。
- **全HTML** → `?v=` ハッシュ更新。同じ階層へ上書き。

---

## 2026-07-10（その16）

### ヒーローを全自動再生化＋スクロールでスキップ＋SCROLL↓を必要時のみ表示
- **`js/main.js`** → `public_html/js/main.js`
  - 失敗→再挑戦→複合→矢印→5行→キャッチを**全て自動再生**（無操作で完走、約16秒）。
  - **自動再生中にスクロール／スワイプすると、一気に THE NEXT STAGE までスキップ**（A案）。
  - キャッチ表示後にスクロールすると本編（下のページ）へ解放。
- **`css/style.css`** → `public_html/css/style.css`
  - SCROLL↓インジケーターを、**自動再生中は非表示 → キャッチ表示時（本編へ進む所）だけ表示**に変更。
- **全HTML** → `?v=` ハッシュ更新。同じ階層へ上書き。

---

## 2026-07-10（その15）

### 5行を「時間差で1行ずつ自動表示」→「完成の2秒後に自動でキャッチ」に
- **`js/main.js`** → `public_html/js/main.js`
  - 5行を1ステップに集約。スクロールで到達したら**行ごとに約1秒差で1行ずつ自動表示**
    （一気に全部ではない）。
  - **全5行が出そろってから約2秒後に、自動でキャッチ（THE NEXT STAGE）へ**遷移（スクロール不要）。
    ※途中で手動スクロールすれば早めにキャッチへ行くことも可。戻る操作で自動遷移はキャンセル。
- **全HTML** → `?v=` ハッシュ更新。同じ階層へ上書き。

---

## 2026-07-10（その14）

### 5行がキャッチにすぐ隠れて「消える」問題を修正
- **`js/main.js`** → `public_html/js/main.js`
  - 全5行表示の直後（約90px先）にキャッチが5行を隠していたため、見えた瞬間に消えていた。
  - 修正：(1) **5行→キャッチの移行を長め（約500px）**にして5行を読める余白を確保。
    (2) **同じ操作（一気スクロール）で5行とキャッチを飛ばさない**（一気に回しても5行で止まる）。
- **全HTML** → `?v=` ハッシュ更新。同じ階層へ上書き。

---

## 2026-07-10（その13）

### 5行の進行を「時間ゲート撤廃・純粋に長さ基準」に
- **`js/main.js`** → `public_html/js/main.js`
  - これまで5行にアニメ待ち(mBusy 260ms)が残っていて「1スクロール＝1行」に固定されていた。
    この時間ゲートを撤廃し、**累積スクロール量だけで進行**（1行＝約200px）。
    大きく速く動かした分は複数行まとめて進む＝完全に長さ基準。
- **全HTML** → `?v=` ハッシュ更新。同じ階層へ上書き。

---

## 2026-07-10（その12）

### 5行を「1行ずつ・長さ基準」で表示に変更（1行あたりのスクロール長さを長め）
- **`js/main.js`** → `public_html/js/main.js`
  - その7の一括（時間差）表示を撤回し、**スクロールの長さに応じて1行ずつ**表示。
    ヒーローのステップを6→10に戻す（⑤〜⑨が5行、⑩キャッチ）。
  - **5行の部分だけ1行あたり約200px**（他のステップは約90px）に設定＝じっくり1行ずつ。
- **全HTML** → `?v=` ハッシュが更新（main.js変更に伴う）。同じ階層へ上書き。
- CSS内容は不変（?vは付け替わる＝再取得されるが問題なし）。

---

## 2026-07-10（その11）

### ヒーローの「RE:VENGE」テキストを削除
- **`index.html`** → `public_html/index.html`（トップのみ）
  - ヒーロー上部の `<p class="p-hero__en">RE:VENGE</p>` を削除。
  - CSS/JSは変更なし（?v=ハッシュも据え置き）。他ページは変更なし。

---

## 2026-07-10（その10）

### キャッシュ対策（スマホで古いJS/CSSが残る問題）
- **全HTMLファイル** → それぞれ同じ階層へ上書き
  - CSS/JSの参照に**キャッシュバスティング** `?v=ハッシュ` を付与
    （例：`css/style.css?v=5f9ec820`・`js/main.js?v=5f9ec820`）。
    ハッシュは style.css / main.js / config.js の内容から自動生成され、**中身が変わると自動でURLが変わる**ので
    ブラウザ（スマホ含む）が確実に最新を取得する。
- 併せて最新の **`css/style.css` / `js/main.js`** も差し替え推奨（内容は前回までの最新）。
- ※初回だけHTMLの再読込（更新）が必要な場合あり。以降はJS/CSS変更時に自動で更新される。

---

## 2026-07-10（その9）

### スマホ（タッチ）もPCと同じスクロール挙動に統一
- **`js/main.js`** → `public_html/js/main.js`（この1ファイルだけ）
  - スマホの「再挑戦以降」を、これまでの1スワイプ＝1ステップから、**指の移動量（長さ）で進行**に変更
    （PCと同じ・約90pxで1ステップ・小さなドラッグでも進む・連続ドラッグで最後まで）。
  - 失敗→再挑戦は従来どおり1スワイプ＝1ステップ（強制）。
  - ローダー（回転）・5行の時間差表示は元々デバイス共通で同じ。
- CSS/HTML変更なし。

---

## 2026-07-10（その8）

### 再挑戦以降を「スクロール量（長さ）」で進む方式に（小スクロールでも進む）
- **`js/main.js`** → `public_html/js/main.js`（この1ファイルだけ）
  - 緩ゾーン（再挑戦→複合→矢印→5行→THE NEXT STAGE）を、1ティック＝1ステップではなく
    **スクロール累積量で進行**。1ステップ＝**約90px**（小さなスクロールでも進む／余りは
    次に持ち越すのでスクロール分を無駄にしない）。連続スクロールでそのまま最後まで。
  - 失敗→再挑戦は従来どおり強制1スクロール＝1ステップ（厳格）。
- CSS/HTML変更なし。

---

## 2026-07-10（その7）

### 5行（知らないことで〜きっかけを）を「1スクロールで時間差一括表示」に
- **`js/main.js`** → `public_html/js/main.js`（この1ファイルだけでOK）
  - これまで5行は1スクロール＝1行ずつだったが、**1スクロールで5行が時間差
    （行ごとに約0.24秒ずらして）まとめて表示**されるように変更。
  - ヒーローのステップを 10 → 6 に集約（①失敗 ②再挑戦 ③複合 ④矢印 ⑤5行一括 ⑥キャッチ）。
  - 戻る操作・キャッチ到達後ロック等は維持。
- CSS/HTML変更なし。

---

## 2026-07-10（その6）

### ローダーを高速1回転化＋ヒーローのスクロールを2ゾーン化
- **`css/style.css`** → `public_html/css/style.css`
  - ローダー：花全体を**0.8秒で1回転**（速度アップ）＋花びらが1巡だけ消える。フェードも0.5sに短縮。
- **`js/main.js`** → `public_html/js/main.js`
  - ローダー：1回転（≈0.85秒）でサイト表示。
  - ヒーローのスクロール制御を2ゾーンに：
    - **失敗→再挑戦：強制1スクロール＝1ステップ**（激しいフリック/惰性でも1つだけ）。
    - **再挑戦→THE NEXT STAGE：ゆるめ**（弱スクロール=1カウント、連続スクロールで一気に到達可）。
    - 緩ゾーンの複合(③)700ms・矢印(④)480ms・5行360ms・キャッチ900msに短縮して連続スクロールが流れるように。
- HTML変更なし（この2ファイルだけ差し替えでOK）。

---

## 2026-07-10（その5）

### ヘッダーを静止ロゴ化＋「会社情報」タブ不具合を修正（重要）
- **`css/style.css`** → `public_html/css/style.css`
  - ヘッダーの花マークを回転＋消えるアニメから**静止**に変更（フッターと同じ静止ロゴ）。
- **全HTMLファイル** → それぞれ同じ階層へ上書き
  - ヘッダーの花マークを静止版に（8枚→単一マーク）。
  - **重要バグ修正**：直前の変更でヘッダーのf-stringが分断され、ナビの
    `{c('company')}` 等が未展開のまま出力→「会社情報」ドロップダウンが表示
    されなくなっていた。f-stringを修正し全ページで正常化（会社概要/代表メッセージ/
    社名の由来が再表示）。
- ※花びら画像(petal-0〜7.webp)はローダーで再び使用します（下記）。

### リロード時に「回転＋花びらが消える」ローダーを表示
- **`js/main.js`** → `public_html/js/main.js`
  - ローダーを約1.8秒表示してから隠す（リロードのたびにアニメが見える）。
- **`css/style.css`**（上記に含む）… ローダーを「花全体が回転しつつ花びらが1枚ずつ消える」動きに。
- **全HTML**（上記に含む）… ローダーは**トップページ（index.html）のヒーロー表示時のみ**設置
  （他ページには出しません）。フォント読込を非ブロック化（`media=print`→`onload`）して
  スクリプト遅延を解消＝ローダーが確実に動作／表示も高速化。
- 使用画像：`assets/img/loader/petal-0〜7.webp`（既存）。

---

## 2026-07-10（その4）

### フッターのロゴをヘッダーと同じ花マークに統一
- **`css/style.css`** → `public_html/css/style.css`
  - フッターロゴを旧 `logo.svg`（8色ドット）から、花マーク＋「Re:venge」に変更（静止）。
- **`assets/img/logo-mark.webp`** → `public_html/assets/img/logo-mark.webp`
  - 最新の花びらから静止の花マークを再合成（フッター用）。
- **全HTMLファイル** → それぞれ同じ階層へ上書き（フッターは全ページ共通のため）。

---

## 2026-07-10（その3）

### 濃いオレンジ（ピンクの横）の消え残りかけらを除去
- **`assets/img/loader/petal-5.webp` / `petal-6.webp`** → `public_html/assets/img/loader/` に上書き
  - オレンジ(petal6)を消したとき隣の黄色(petal5)側に残っていたオレンジの小片
    （約100px）を、オレンジ側へ編入（266px移動）。残渣 約14px まで低減。
  - 黄色・オレンジとも単体で完全な花びし・消去時もクリーンを確認。
- CSS/HTML変更なし（この2枚だけ差し替えでOK）。

---

## 2026-07-10（その2）

### 完成形の5秒維持を削除し、連続で1枚ずつ消える動きに
- **`css/style.css`** → `public_html/css/style.css`（これ1ファイルだけでOK）
  - 「完成形を約5秒維持」を廃止。花びらが1枚ずつ、ふわっと消えて戻る動きを
    時計回りに**連続**で（維持なし・常に欠けは1枚）。1周7.2秒。花全体の回転は継続。
  - 検証：全部そろう時間＝0秒、同時に欠けるのは常に1枚。
  - 補足：一番上（青の横）のピンクと左上（オレンジの横）のマゼンタは、単体では
    各々完全な花びらで、両方消せば上部は空になることを確認済み（分割は正常）。
    維持をなくしたことで各花びらの消失がはっきり見えます。

---

## 2026-07-10

### 「消えながら回転」＋ピンク2枚の分割やり直し（断片化・消え残り解消）
- **`css/style.css`** → `public_html/css/style.css`
  - ヘッダーの花全体を**ゆっくり回転**（12秒で1回転）させつつ、花びらが1枚ずつ
    消える動きに変更（`markSpin`）。reduced-motionでは回転も停止。
- **`assets/img/loader/petal-0〜7.webp`（8枚）** → `public_html/assets/img/loader/`
  - 2つのピンク（上・左上）が別々の連結成分であることを利用して**連結成分で分割し直し**。
    これまでの角度分割で起きていた断片化／付け根の消え残り／「一番上の花びらが
    消えない」現象を解消。上ピンクは付け根ごと綺麗に消えます。
- HTML変更なし。

---

## 2026-07-09（その6）

### 一番上の赤っぽいピンクの「付け根」を本体と一緒に消えるように
- **`assets/img/loader/petal-0.webp` / `petal-7.webp`** → `public_html/assets/img/loader/` に上書き
  - 上ピンク（petal0）の中心側の付け根が、隣のマゼンタ側レイヤーに入っていて
    一緒に消えなかった問題を修正。付け根の該当画素（約3,700px）を上ピンクへ編入。
  - これで上ピンクが欠けるとき、付け根も一緒に消えます。マゼンタ側は正常（確認済み）。
- CSS/HTML変更なし。実質 petal-0/petal-7 の2枚差し替えでOK
  （不安なら8枚まとめてでも可）。

---

## 2026-07-09（その5）

### 花びらの「余（残渣）」を精査・除去（分割やり直し）
- **`assets/img/loader/petal-0〜7.webp`（8枚）** → `public_html/assets/img/loader/` に上書き
  - 濃いオレンジ・ピンク系2枚で欠けたときに残っていた破片を解消するため、
    高解像度の元ロゴから分割し直し（色相＋角度で領域分割、角度重み最適化wa=0.22）。
  - さらに各花びらの**ドロップシャドウ（影）を除去**し、影の幽霊も解消。
  - 検証（各花びらを消して残色を計測）：青/紫/緑/黄緑/マゼンタ＝残渣0。
    上ピンク・オレンジはごく微小な破片が残るが、これは元絵で2枚のピンクが
    物理的に重なって描かれているため（ヘッダー34px表示では視認不可レベル）。
- CSS/HTMLの変更なし（この8枚だけ差し替えればOK）。

---

## 2026-07-09（その4）

### ヘッダーの花の動きを調整（ゆっくり・1枚ずつ・完成形を5秒維持）
- **`css/style.css`** → `public_html/css/style.css`（これ1ファイルだけでOK）
  - 速すぎ／半分欠ける現象を解消。`petalGlow` を「花びらが1枚ずつふわっと
    フェードインして一周 → 完成形を約5秒維持 → 繰り返し」に変更。
  - 1周 = 9.5秒（うち全花びらが揃った完成形を約5秒キープ）。常に欠けは1枚だけ。
  - HTMLの変更なし（花びら要素は前回のまま）。

---

## 2026-07-09（その3）

### ヘッダーの花マークを「回すだけ」→「花びらが1枚ずつ欠ける」に変更
- **`css/style.css`** → `public_html/css/style.css`
  - ヘッダーの花を1枚画像の回転から、8枚レイヤーの `petalDip`（花びらが1枚ずつ
    欠けて巡回）に変更。画像はローダー用の `assets/img/loader/petal-0〜7.webp` を再利用。
- **全HTMLファイル** → それぞれ同じ階層へ上書き（ヘッダーは全ページ共通）
  - ヘッダーの花マークを8枚の花びら要素に変更（`<i class="l-header__petal">`×8）。
  - 対象は前回（その2）と同じ全HTML。
- 追加アセットなし（`petal-0〜7.webp` は既にサーバー上にある想定）。

---

## 2026-07-09（その2）

### ヘッダーロゴを回転する花マークに変更＋ローダー廃止＋フッターのホバー虹
- **`css/style.css`** → `public_html/css/style.css`
  - ヘッダーのアイコンを花ロゴに変更し、ループで回転（`markSpin`）。
  - フッターのリンクをホバー時に虹色テキストで流れるように（ヘッダーと同様）。
- **`assets/img/logo-mark.webp`**（新規）→ `public_html/assets/img/logo-mark.webp`
  - ヘッダーの回転用・花マーク画像。
- **全HTMLファイル** → それぞれ同じ階層へ上書き（ヘッダーは全ページ共通のため）
  - `index.html`（＋ヒーロー上の全画面ローダーを削除）
  - `contact.html` / `message.html` / `news.html` / `privacy.html` / `profile.html`
    / `purpose.html` / `security.html` / `services.html` / `sitepolicy.html`
  - `service/service-01.html` / `service/service-02.html` / `service/service-03.html`
  - `news/news-20260707.html`
  - 変更点：ヘッダーのロゴを「回転する花マーク＋Re:venge」に統一。

> ※HTMLは全ページでヘッダーが変わるため全置換が確実です。まとめて差し替えるなら
>   同梱の静的ZIP（WPなし）を展開して上書きするのが早いです。

---

## 2026-07-09

### お問い合わせメールの文字化け修正
- **`contact.php`** → アップロード先: `public_html/contact.php`
  - 症状: 受信メール本文が「?」だらけ（例: `????????????Revenge`）。
  - 原因: `mb_send_mail()` + `mb_language('Japanese')` が本文をISO-2022-JP（JIS）へ
    変換していたのに、ヘッダーは `charset=UTF-8` と宣言 → 不一致で文字化け。
  - 対応: 全体をUTF-8に統一。`mb_send_mail`を廃止し、件名はUTF-8のMIMEエンコード
    （`=?UTF-8?B?...?=`）、本文はUTF-8のまま `mail()` で送信。`Content-Transfer-Encoding: 8bit`
    を明示。（件名・本文の往復デコード＝一致を確認済み）
